import React from 'react';

import { MainTitle, Wrapper } from './styles';

const Header = () => {
  return (
    <Wrapper>
      <MainTitle>Fake Store</MainTitle>
    </Wrapper>
  );
};

export default Header;
